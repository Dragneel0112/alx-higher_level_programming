-- Lists all rows of the table second_table
-- Results display score and name and is ordered by score
-- Database passed with argument in mysql
SELECT score, name FROM second_table ORDER BY score DESC;
